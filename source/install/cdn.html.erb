<% @menu_path = '/install/menu' %>

<h1>
  <%= breadcrumb_link 'Install', '/install' %>
  <%= page_title 'Link to a CDN' %>
</h1>

<div class="prose">

  <p>
    You can link to the Unpoly files directly from a CDN:
  </p>

  <pre><code><%= h cdn_js_include('unpoly.min.js') %>
<%= h cdn_css_include('unpoly.min.css') %></code></pre>

  <p>
    If you're <a href="/changes/upgrading">upgrading from an older version</a> and need the <code>unpoly-migrate.js</code> shim,
    you can load it from the CDN as well:
  </p>

  <pre><code><%= h cdn_js_include('unpoly-migrate.min.js') %></code></pre>

  <h3>Adding Bootstrap integration</h3>

  <p>If you want to use Unpoly's Bootstrap integration, you should also add <b>one</b> of the following links for your Bootstrap version:</p>

  <pre><code><%= h cdn_css_include('unpoly-bootstrap3.min.css') %>
<%= h cdn_css_include('unpoly-bootstrap4.min.css') %>
<%= h cdn_css_include('unpoly-bootstrap5.min.css') %></code></pre>

  <%= partial 'star_prompt' %>

</div>
