up.util.assign up.modal.config,
  openAnimation: 'zoom-in'
  closeAnimation: 'zoom-out'
  openDuration: 300
  closeDuration: 300

up.modal.flavors.fullscreen =
  openDuration: 500
  closeDuration: 500

up.dom.config.fallbacks = ['.screen', 'body']
up.dom.config.fallbackTransition = 'cross-fade'

