---
title: 'Modal dialogs'
explanation: |
  Give a link an `[up-layer=new]` attribute to extract a CSS selector from the destination
  and open the fragment in a modal dialog.
  No server-side changes required.
highlight:
order:
  - 'preview.html'
  - 'full.html'
