---
explanation: |
  Give a link an `up-modal` attribute to extract a CSS selector from the destination
  and open the fragment in a modal dialog.
  No server-side changes required.
highlight:
  - 'up-modal'
order:
  - 'preview.html'
  - 'full.html'
